const users = {
    name: "Rohim",
    age: 28,
    friends: ["bd", "india"],
    country: "Bangladesh",
    family: {
        fatherName: "korim Khan",
        motherName: "Asma Begum",
    }
}

const keys = Object.keys(users); //object ar sob gulo key return korbe. 
console.log(keys);

const valu = Object.values(users); //object ar sob values gulo return korbe.
console.log(valu);